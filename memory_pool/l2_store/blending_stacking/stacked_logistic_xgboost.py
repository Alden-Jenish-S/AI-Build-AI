'''\nStacking ensemble (Logistic Regression + XGBoost) for Playground Series S6E2.\nProvides a single callable entrypoint `fit_predict_stacked` that trains on\n(train_df, target_col) and returns predictions for test_df.\n'''\n\nfrom __future__ import annotations\n\nimport numpy as np\nimport pandas as pd\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.model_selection import StratifiedKFold\nfrom sklearn.preprocessing import StandardScaler\nfrom xgboost import XGBClassifier\nfrom typing import Tuple\n\n\ndef _prepare_xy(df: pd.DataFrame, target_col: str) -> Tuple[np.ndarray, np.ndarray]:\n    '''Separate features and target, return as numpy arrays.'''\n    X = df.drop(columns=[target_col]).values\n    y = df[target_col].values.astype(int)\n    return X, y\n\n\ndef _scale_features(X_train: np.ndarray, X_valid: np.ndarray) -> Tuple[np.ndarray, np.ndarray]:\n    '''Fit StandardScaler on training data and transform both sets.'''\n    scaler = StandardScaler()\n    X_train_scaled = scaler.fit_transform(X_train)\n    X_valid_scaled = scaler.transform(X_valid)\n    return X_train_scaled, X_valid_scaled, scaler\n\n\ndef fit_predict_stacked(\n    train_df: pd.DataFrame,\n    test_df: pd.DataFrame,\n    target_col: str = \"target\",\n    n_splits: int = 5,\n    random_state: int = 42,\n) -> np.ndarray:\n    '''\n    Train a stacked LogisticRegression + XGBoost model and return test predictions.\n\n    Parameters\n    ----------\n    train_df : pd.DataFrame\n        Training data containing features and the target column.\n    test_df : pd.DataFrame\n        Test data (features only – target column must be absent).\n    target_col : str, default \"target\"\n        Name of the target column in ``train_df``.\n    n_splits : int, default 5\n        Number of stratified folds for generating out‑of‑fold predictions.\n    random_state : int, default 42\n        Seed for reproducibility.\n\n    Returns\n    -------\n    np.ndarray\n        Predicted probabilities for the positive class (shape = n_test_samples).\n    '''\n    # ------------------------------------------------------------------ #\n    # 1. Prepare data\n    # ------------------------------------------------------------------ #\n    X, y = _prepare_xy(train_df, target_col)\n    X_test = test_df.values  # assume test_df has same column order as train_df minus target\n\n    # ------------------------------------------------------------------ #\n    # 2. Initialize base learners\n    # ------------------------------------------------------------------ #\n    logreg = LogisticRegression(\n        penalty=\"l2\",\n        C=1.0,\n        solver=\"lbfgs\",\n        max_iter=1000,\n        random_state=random_state,\n    )\n    xgb = XGBClassifier(\n        n_estimators=300,\n        learning_rate=0.05,\n        max_depth=4,\n        subsample=0.9,\n        colsample_bytree=0.9,\n        objective=\"binary:logistic\",\n        eval_metric=\"logloss\",\n        n_jobs=-1,\n        random_state=random_state,\n        tree_method=\"hist\",\n    )\n\n    # ------------------------------------------------------------------ #\n    # 3. Generate out‑of‑fold predictions for each base learner\n    # ------------------------------------------------------------------ #\n    oof_logreg = np.zeros(len(X))\n    oof_xgb = np.zeros(len(X))\n    test_logreg = np.zeros(len(X_test))\n    test_xgb = np.zeros(len(X_test))\n\n    skf = StratifiedKFold(n_splits=n_splits, shuffle=True, random_state=random_state)\n\n    for fold_idx, (train_idx, valid_idx) in enumerate(skf.split(X, y)):\n        X_tr, X_val = X[train_idx], X[valid_idx]\n        y_tr, y_val = y[train_idx], y[valid_idx]\n\n        # Scale inside each fold to avoid leakage\n        X_tr_scaled, X_val_scaled, scaler = _scale_features(X_tr, X_val)\n        X_test_scaled = scaler.transform(X_test)\n\n        # ---- Logistic Regression ---- #\n        logreg.fit(X_tr_scaled, y_tr)\n        oof_logreg[valid_idx] = logreg.predict_proba(X_val_scaled)[:, 1]\n        test_logreg += logreg.predict_proba(X_test_scaled)[:, 1] / n_splits\n\n        # ---- XGBoost ---- #\n        xgb.fit(X_tr, y_tr)  # XGBoost handles raw features; scaling not required\n        oof_xgb[valid_idx] = xgb.predict_proba(X_val)[:, 1]\n        test_xgb += xgb.predict_proba(X_test)[:, 1] / n_splits\n\n    # ------------------------------------------------------------------ #\n    # 4. Build meta‑features (out‑of‑fold predictions) and train meta‑learner\n    # ------------------------------------------------------------------ #\n    meta_X_train = np.vstack([oof_logreg, oof_xgb]).T  # shape: (n_samples, 2)\n    meta_X_test = np.vstack([test_logreg, test_xgb]).T  # shape: (n_test_samples, 2)\n\n    meta_learner = LogisticRegression(\n        penalty=\"l2\",\n        C=1.0,\n        solver=\"lbfgs\",\n        max_iter=1000,\n        random_state=random_state,\n    )\n    meta_learner.fit(meta_X_train, y)\n\n    # ------------------------------------------------------------------ #\n    # 5. Final predictions on test set\n    # ------------------------------------------------------------------ #\n    test_proba = meta_learner.predict_proba(meta_X_test)[:, 1]\n    return test_proba\n\n\n# ---------------------------------------------------------------------- #\n# Interface entrypoint expected by the builder\n# ---------------------------------------------------------------------- #\ndef fit_predict_stacked_entrypoint(\n    train_df: pd.DataFrame,\n    test_df: pd.DataFrame,\n    target_col: str = \"target\",\n) -> np.ndarray:\n    '''\n    Builder‑friendly wrapper that matches the expected signature:\n        (train_df, test_df, target_col) -> np.ndarray of predicted probabilities.\n\n    This function simply forwards to the core implementation.\n    '''\n    return fit_predict_stacked(train_df, test_df, target_col=target_col)\n